/* eslint-disable */
import fs from 'fs';
const a = 1;

const dd = 5;
const zloong = 6;
const cc = 7;
// console.log(a);
const v = 1;
const b = 'ab';
const c = 'dd';

const ddd = new Set();

const person = {
  name: 'Fred',
  age: 87,
};
Object.values(person); // ['Fred', 87]

const add = async (a, b) => {
  const c = a + 1;

  return b + a + c;
};

const result = await add();

const http = require('http');
const gca = window.document.getElementById('cc');

const tt = 1;
if (tt == 1) {
  console.log('oooo00');
  aaa;
}
